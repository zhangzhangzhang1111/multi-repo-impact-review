# scriptswtlua 代码变更影响评审

> **评审结论：通过｜风险：中**  
> 本次修复纠正两融担保品买入、卖出盘后定价委托参数在延迟组装前被提前清空的问题；未发现阻断性代码缺陷，建议补齐顺序回归测试。

## 1. 评审信息

| 项目 | 内容 |
|---|---|
| 项目 / 需求 | scriptswtlua / SJCGZZ-21023 缓存清除 |
| 评审范围 | `4de66dfb` → `2c9bee33`；1 个 Lua 文件、2 个业务函数 |
| 影响业务 | 融资融券担保品买入、担保品卖出的盘后定价委托确认 |
| 评审结果 | 0 个阻断缺陷；1 项 P2 测试覆盖建议；允许合入 |
| 核验情况 | Shell 与 PowerShell 流程均通过；完成静态调用链和源码复核；未连接真实柜台、未做客户端联调 |

**本次结论**

- **影响什么**：盘后定价委托确认时，已确认的委托策略应进入本次柜台请求，并在使用后清理，避免跨请求残留。
- **发现什么**：改动的执行顺序符合上述目标；官方图谱漏掉了 Lua 变量分派边，AI 已依据源码补全并核验到客户端请求入口。
- **合入条件**：无阻断条件；上线前执行下列必测场景。

## 2. 业务影响与测试范围

| 业务场景 | 本次影响 | 测试级别 | 预期结果 |
|---|---|---|---|
| 两融账户提交担保品买入的创业板盘后定价委托 | 本次确认的盘后委托策略被正确带入下单请求，使用后不残留 | 必测 | 委托按盘后定价方式受理；随后发起普通委托时不沿用上次策略 |
| 两融账户提交担保品卖出的创业板盘后定价委托 | 本次确认的盘后委托策略被正确带入下单请求，使用后不残留 | 必测 | 委托按盘后定价方式受理；随后发起普通委托时不沿用上次策略 |
| 北交所或港股通证券尝试盘后定价委托 | 拦截逻辑未变，但与本次状态清理相邻 | 建议回归 | 仍提示该品种不支持，不产生柜台委托 |
| 两融普通担保品买入、卖出 | 普通委托流程不应被盘后状态影响 | 建议回归 | 普通委托参数与结果保持原有行为，无上一次盘后委托信息残留 |

**明确不受影响**

- 融资买入、融券卖出、担保品划转等其他两融交易类型无需专项回归。
- 柜台应答展示、成交回报和查询页面没有直接改动。

## 3. 代码审核结果

> **确认缺陷 0 项；改进建议 1 项：P2 1 项。** 当前代码可以合入，建议用自动化用例固定“先取值、后清理”的顺序。

### P2-01｜延迟组装与缓存清理顺序缺少回归用例

| 开发关注项 | 内容 |
|---|---|
| 代码位置 | `deploy/project/RongZiRongQuan/wt_rzrq_func.lua:6750-6758`、`:7351-7359` |
| 具体问题 | 本次正确地把清理动作移到请求内容的延迟组装函数内，但仓库中没有覆盖这两个确认函数的测试。若以后再次把清理移到延迟组装之前，代码仍能运行，却会静默丢失盘后委托策略。 |
| 影响 | 盘后定价委托可能退化为普通委托参数，属于交易语义错误，人工走查不容易发现。 |
| 修改方式 | 为担保品买入、卖出各增加参数组装测试：先设置盘后策略，执行实际序列化/延迟求值，断言本次请求包含对应策略，再断言会话缓存已清空；另各补一条普通委托不携带旧策略的用例。 |
| 完成标准 | 4 个用例可重复通过；把清理语句移到延迟求值之前时，盘后策略用例必须失败。 |

<details>
<summary>技术证据（调用链、图谱与机器产物）</summary>

- 官方 `codebase-memory-mcp 0.10.2` 产物：3274 个节点、15000 条边；目标文件无已记录解析问题。
- 官方 `detect_changes` 找到 2 个变更符号，但报告 0 个上游影响；对两个变更函数各执行深度 1、上限 30 的反向追踪，均返回 0 个调用者。
- AI 源码核验补全 Lua 变量分派：
  - `OnHandleClientReqMsg` → `DoSubProcessFunction`（静态边，已核验）；
  - `DoSubProcessFunction` → `DoSubProcessFunction_RongZiRongQuan`（静态边，已核验）；
  - 页面 1993 / 1995 将两个变更函数赋给局部函数变量，再统一调用（动态图谱漏边，已补充）。
- 调用链从深度 1 按需扩展到深度 2；核心唯一节点 5 个，低于全局预算 120，未截断，未扩展到硬上限 4。
- 路径状态：`fully-verified`；图谱原始结果存在 2 条动态分派漏边，已由源码证据修正。
- 未执行真实柜台、移动客户端和券商配置联调，运行结果仍需在测试环境确认。

</details>
