{
  "mcpServers": {
    "codebase-memory-mcp": {
      "command": "sh",
      "args": [
        "-c",
        "plugin_root=.; if [ -n \"$CLAUDE_PLUGIN_ROOT\" ]; then plugin_root=$CLAUDE_PLUGIN_ROOT; fi; exec \"$plugin_root/runtime/launch-mcp.sh\""
      ],
      "cwd": "."
    }
  }
}
